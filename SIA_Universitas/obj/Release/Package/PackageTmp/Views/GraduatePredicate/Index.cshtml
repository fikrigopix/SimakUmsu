@model IEnumerable<SIA_Universitas.Models.Mstr_Graduate_Predicate>

@{
    ViewBag.Title = "GraduatePredicate";
}

<h3>Predikat Lulus</h3>
<div class="panel panel-default bootstrap-admin-no-table-panel">
    <div class="panel-heading">
        <div class="pull-right">
            <a href="@Url.Action("Create")" class="btn-sm btn-success" style="font-style:italic;font-size:medium"><i class="icon-plus-sign"></i> Tambah data</a>
        </div>
        <div class="bootstrap-admin-box-title right text-success">
            <b>Master Data</b>
        </div>
    </div>
    <div class="bootstrap-admin-panel-content bootstrap-admin-no-table-panel-content collapse in">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Graduate_Predicate_Code)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Predicate_Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Predicate_Name_Eng)
                </th>
                <th><i class="glyphicon glyphicon-cog" style="color:black"></i></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Graduate_Predicate_Code)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Predicate_Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Predicate_Name_Eng)
                    </td>
                    <td align="center" width="20%">
                        <a href="@Url.Action("Edit", new { id = item.Graduate_Predicate_Id })" title="Ubah" class="btn-xs btn-warning">
                            Edit <i class="glyphicon glyphicon-edit" style="color:white"></i>
                        </a>&nbsp;
                        <a href="@Url.Action("Details", new { id = item.Graduate_Predicate_Id })" title="Rincian" class="btn-xs btn-info">
                            Rincian <i class="glyphicon glyphicon-list-alt" style="color:white"></i>
                        </a>&nbsp;
                        <a href="@Url.Action("DeleteConfirmed", new { id = item.Graduate_Predicate_Id })" title="Hapus" class="btn-xs btn-danger confirmation">
                            Hapus <i class="glyphicon glyphicon-trash" style="color:white"></i>
                        </a>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
@section Scripts{
    <script>
        function sebSweetConfirm(originLink) {
            swal({
                title: 'Anda yakin?',
                text: "Akan menghapus data ini!",
                type: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Ya, hapus!',
                cancelButtonText: 'Batal',
                //closeOnConfirm: true,
                //closeOnCancel: true
            }).then(function (isConfirm) {
                if (isConfirm) {
                    window.location.href = originLink;
                }
            })
        }

        $('.confirmation').click(function (event) {
            event.preventDefault();
            var originLink = $(this).attr("href");
            sebSweetConfirm(originLink);
        });
    </script>
    @if (ViewBag.berhasilHapus != null)
    {
        <script>

            $(document).ready(function sweet() {
                swal('Selesai!', '@ViewBag.berhasilHapus', 'success');
            });

        </script>

    }
    @if (ViewBag.gagalHapus != null)
    {
        <script>

            $(document).ready(function sweet() {
                swal('Kesalahan!', '@ViewBag.gagalHapus', 'error');
            });

        </script>

    }
}
